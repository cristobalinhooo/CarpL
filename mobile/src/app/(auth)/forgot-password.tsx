import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ForgotPasswordScreen() {
  return (
    <ScreenPlaceholder
      title="Recuperar contraseña"
      note="La fase de Auth implementa este formulario contra Supabase Auth."
    />
  );
}
