import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function VehiclesScreen() {
  return (
    <ScreenPlaceholder
      title="Vehículos"
      note="Agregar por patente o manual, confirmar datos recuperados (Technical Spec §12.3) — pendiente de fase futura."
    />
  );
}
