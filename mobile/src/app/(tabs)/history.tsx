import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HistoryScreen() {
  return (
    <ScreenPlaceholder
      title="Historial"
      note="Listar y filtrar investigaciones, paginación (Technical Spec §12.3) — pendiente de fase futura."
    />
  );
}
