import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="Inicio"
      note="Vehículo activo, CTA de investigación y recientes (Technical Spec §12.3) — pendiente de fase futura."
    />
  );
}
