# Mobile — React Native + Expo

Vacía hasta que arranque el frontend (después de tener endpoints reales
que consumir). Estructura de referencia, Technical Spec §17.1:

```
src/components, screens, navigation, hooks, services, api, assets,
    types, utils, constants
```

Mapa de pantallas y flujo principal: Technical Spec §12.3/§12.4.
Mockups de referencia: `../docs/design/mockups/`.
